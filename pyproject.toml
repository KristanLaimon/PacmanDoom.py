[project]
name = "doom-search"
version = "0.1.0"
description = "DOOM-style maze game with visible A* and BFS search using pygame."
requires-python = ">=3.11,<3.14"
dependencies = [
    "pygame>=2.6.1",
]

[tool.uv]
package = false
